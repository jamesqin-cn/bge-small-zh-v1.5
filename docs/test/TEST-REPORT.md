# test-report.md

# memory-lancedb 插件测试报告

**测试时间**: _________  
**测试人员**: _________  
**服务版本**: bge-small-zh-v1.5  

---

## 一、服务健康检查

### 1.1 服务状态

```bash
curl http://localhost:7078/health
```

| 检查项 | 预期值 | 实际值 | 状态 |
|--------|--------|--------|------|
| status | "ok" |  |  |
| model | "bge-small-zh-v1.5" |  |  |
| dimensions | 512 |  |  |
| ready | true |  |  |
| port | 7078 |  |  |

**结论**: □ 通过  □ 失败  □ 警告

---

## 二、向量生成测试

### 2.1 单条文本测试

```bash
curl -X POST http://localhost:7078/v1/embeddings \
  -H "Content-Type: application/json" \
  -d '{"input": "测试文本"}'
```

| 检查项 | 预期值 | 实际值 | 状态 |
|--------|--------|--------|------|
| 返回 data 数组 | 是 |  |  |
| 向量维度 | 512 |  |  |
| 向量已归一化 | 是 |  |  |
| 生成时间 | < 500ms |  |  |

**结论**: □ 通过  □ 失败  □ 警告

### 2.2 批量文本测试

```bash
curl -X POST http://localhost:7078/v1/embeddings \
  -H "Content-Type: application/json" \
  -d '{"input": ["文本 1", "文本 2", "文本 3"]}'
```

| 检查项 | 预期值 | 实际值 | 状态 |
|--------|--------|--------|------|
| 返回 3 个向量 | 是 |  |  |
| 每个向量维度 | 512 |  |  |
| 生成时间 | < 1500ms |  |  |

**结论**: □ 通过  □ 失败  □ 警告

---

## 三、autoCapture 测试

### 3.1 触发场景测试

| 场景 | 测试文本 | 向量生成 | 日志记录 | 状态 |
|------|----------|----------|----------|------|
| 个人信息 | "我叫张三，30 岁" |  |  |  |
| 偏好设置 | "我喜欢 TypeScript" |  |  |  |
| 重要事实 | "我住在北京" |  |  |  |
| 计划承诺 | "下周完成开发" |  |  |  |
| 情感状态 | "最近感觉很累" |  |  |  |

### 3.2 验证方法

```bash
# 查看日志
tail -f logs/combined.log
```

**日志检查项**:
- [ ] 有 "开始生成向量" 记录
- [ ] 有 "向量生成成功" 记录
- [ ] requestId 一致
- [ ] count 为 1

**结论**: □ 通过  □ 失败  □ 警告

---

## 四、autoRecall 测试

### 4.1 记忆存储

```bash
# 存储记忆
curl -X POST http://localhost:7078/v1/embeddings \
  -H "Content-Type: application/json" \
  -d '{"input": "用户名叫王五，35 岁，喜欢喝茶"}'
```

**检查项**:
- [ ] 向量生成成功
- [ ] 维度为 512
- [ ] 日志记录完整

### 4.2 记忆召回

```bash
# 查询记忆
curl -X POST http://localhost:7078/v1/embeddings \
  -H "Content-Type: application/json" \
  -d '{"input": "用户叫什么名字？"}'
```

**检查项**:
- [ ] 向量生成成功
- [ ] 能计算相似度
- [ ] 召回结果合理

**结论**: □ 通过  □ 失败  □ 警告

---

## 五、性能测试

### 5.1 响应时间

| 测试 | 预期 | 实际 | 状态 |
|------|------|------|------|
| 单条生成 | < 500ms |  |  |
| 10 条批量 | < 2000ms |  |  |
| 100 条批量 | < 10000ms |  |  |

### 5.2 资源占用

| 指标 | 预期 | 实际 | 状态 |
|------|------|------|------|
| 内存占用 | ~100MB |  |  |
| CPU 占用 | < 50% |  |  |

**结论**: □ 通过  □ 失败  □ 警告

---

## 六、语义相似度测试

### 6.1 相似文本

```bash
# 测试相似文本
curl -X POST http://localhost:7078/v1/embeddings \
  -H "Content-Type: application/json" \
  -d '{"input": ["我喜欢打篮球", "我热爱篮球运动"]}'
```

**相似度**: _______ (预期 > 0.8)

### 6.2 不相似文本

```bash
# 测试不相似文本
curl -X POST http://localhost:7078/v1/embeddings \
  -H "Content-Type: application/json" \
  -d '{"input": ["我喜欢打篮球", "我喜欢吃火锅"]}'
```

**相似度**: _______ (预期 < 0.5)

**结论**: □ 通过  □ 失败  □ 警告

---

## 七、日志检查

### 7.1 日志完整性

```bash
tail -50 logs/combined.log
```

**检查项**:
- [ ] 所有请求都有 requestId
- [ ] 错误日志正确记录
- [ ] 时间戳准确
- [ ] JSON 格式正确

### 7.2 错误日志

```bash
cat logs/error.log
```

**错误数量**: _______

**结论**: □ 通过  □ 失败  □ 警告

---

## 八、总体评估

### 8.1 测试总结

| 测试项 | 状态 |
|--------|------|
| 服务健康检查 |  |
| 向量生成 |  |
| autoCapture |  |
| autoRecall |  |
| 性能测试 |  |
| 语义相似度 |  |
| 日志检查 |  |

### 8.2 问题记录

| 序号 | 问题描述 | 严重程度 | 状态 |
|------|----------|----------|------|
| 1 |  |  |  |
| 2 |  |  |  |

### 8.3 最终结论

□ **通过** - 所有测试项正常，可以投入使用  
□ **有条件通过** - 存在小问题但不影响核心功能  
□ **失败** - 存在严重问题，需要修复后重新测试

---

## 九、下一步行动

- [ ] 修复发现的问题
- [ ] 重新运行测试
- [ ] 更新配置
- [ ] 部署到生产环境

---

**测试人员签名**: _________  
**审核人员签名**: _________  
**日期**: _________
